import { parseCFG } from './src/engines/grammar/parser'; import { convertToCNF } from './src/engines/grammar/cnf'; import { convertToGNF } from './src/engines/grammar/gnf'; import { generateWords } from './src/engines/grammar/generator'; try { const cfg = parseCFG('S - S b | \\epsilon'); console.log('Original CFG:', cfg); const cnf = convertToCNF(cfg); console.log('CNF:', cnf); const gnf = convertToGNF(cfg); console.log('GNF:', gnf); const words = generateWords(cfg, 5); console.log('Sampler words:', words); } catch (e) { console.error('Error:', e); }  
